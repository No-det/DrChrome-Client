import './index.css';

const Loader = (props) => {
    return (
        <div className="loading">
            <p>Loading . . .</p>
            <div className="loader"></div>
        </div>
    );
}

export default Loader;
